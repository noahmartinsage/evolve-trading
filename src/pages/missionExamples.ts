/**
 * 任务页输入框下面的三个例句。
 *
 * ── 它们为什么**不是**随便写三句漂亮话 ────────────────────────────────
 * 用户第一次打开这一页时，唯一会做的动作就是"点一个例句、按裁定"。
 * 三个例句的结论，就是他对这一页的全部第一印象。
 *
 * 三条必须**各指向一条支路**（可行 / 证据不足 / 做不成）：
 *   · 三条都落"做不成" ⇒ 用户看到三次"这也不行"⇒ 结论是**这一页坏了**
 *     （实测发生过：三条例句分别是 10 倍/1 天、沙盒、3 天翻倍，**全部** `infeasible`）；
 *   · 三条都落"可行"   ⇒ 用户以为下单很随意，而真实的第一道门恰恰是"目标越界"。
 *
 * ★ 为什么这层意思要住在 `.ts` 里，而不是留在 `MissionPage.tsx` 的注释里：
 *   `scripts/mission-smoke.ts` 的 S-M3b 会**真的把这三句话喂进裁定器**，
 *   断言它们仍然三态互不相同。写进注释的话，题面与判据一旦分岔
 *   （改上限、换场所、调阈值）没有任何东西会报红 —— 而分岔之后
 *   变的是**用户看到的结论**，页面本身看起来完全正常。
 *
 * ── 每一句的实测结论（2026-09-23，`baseEnv()` = okx-testnet 已接）────
 *   ① 用2000U在OKX测试网做到2200U，7天内
 *      ⇒ feasible（+10% 在 0~+50% 内；只有三条 warn，不阻断）
 *   ② 把BTCUSDT的10U做到11U，24小时
 *      ⇒ unverifiable（`hold:MISSING_SLOTS`，缺 venue —— 教的是"补一句话就能继续"）
 *   ③ 用10U在OKX测试网做到100U，1天内
 *      ⇒ infeasible（`block:TARGET_ABOVE_AUTOPILOT_RANGE`，+900% 远超上限）
 */
export const MISSION_EXAMPLES: readonly string[] = [
  '用2000U在OKX测试网做到2200U，7天内',
  '把BTCUSDT的10U做到11U，24小时',
  '用10U在OKX测试网做到100U，1天内',
]
