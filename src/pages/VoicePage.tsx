/**
 * ⚠️ 已合并 —— 本文件不再是页面。
 *
 * 「语音管家」与「悬浮桌宠」原本是两个页面、**两套会话实现**（配置加载、播报去重、
 * 说话、打断、回声判定、合成器接线各写一遍）。现在合并为
 * `src/pages/VoiceHubPage.tsx`：同一个组件，两种布局
 * （`?pet=1` → 悬浮窗；常规渲染 → 控制台 + 内嵌头像）。
 *
 * 保留这个文件只为**重定向**：万一还有旧引用，它拿到的是合并后的页面，
 * 而不是一份会慢慢腐烂的第二实现 —— 那正是这次合并要消灭的东西。
 *
 * `scripts/pet-smoke.ts` 的 P12 组钉住这一点：本文件里不得再出现任何状态、
 * 请求、JSX 或业务常量，只允许下面这一行 re-export。
 *
 * 注：本机安全策略拒绝删除仓库文件（safe-delete 走回收站失败后 fail-closed），
 * 所以用**重定向**而不是删除来表达"已合并"，不去绕过那条策略。
 */
export { default } from './VoiceHubPage.tsx'
